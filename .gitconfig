[core]
	editor = nvim
[alias]
	a = add
	b = branch
	c = commit
	d = diff
	l = log
	p = push
	s = status
	st = status -uno
	curr = branch --show-current
	lc = "!fn() { \
		git log -\"$1\" --oneline \
		--cherry-mark --left-right \
		\"$2\"; \
		}; fn"
[diff]
	tool = nvimdiff
[branch]
	sort = committerdate
[column]
	ui = auto
[user]
	name = bazko1
